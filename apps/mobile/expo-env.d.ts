/// <reference types="expo/types" />

// Gerado pela Expo. Traz os tipos do ambiente — entre eles a declaração
// de `process.env`, que é como as variáveis EXPO_PUBLIC_* são lidas em
// lib/supabase.ts. Não editar à mão.
